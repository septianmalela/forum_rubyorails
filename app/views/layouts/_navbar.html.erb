<nav class="navbar navbar-expand-lg navbar-light bg-light">

      <% unless current_user && current_user.role == 'admin' %>
          <%= link_to "Forum", root_path, class: 'navbar-brand' %>
      <% if user_signed_in? %>
          <%= link_to "Profile", users_path, class: 'navbar-brand' %>
      <% end %>
        <%else%>
          <%= link_to "Forum", root_path, class: 'navbar-brand' %>
          <%= link_to "Admin", admin_index_path, class: 'navbar-brand' %>
          <% link_to "Admin", table_post_admin_index_path, class: 'navbar-brand' %>
          <% link_to "Admin", table_user_admin_index_path, class: 'navbar-brand' %>
          <% link_to "Admin", table_like_admin_index_path, class: 'navbar-brand' %>
        <%end%>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= user_signed_in? ? current_user.name : "Hallo Guest" %>
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <% if user_signed_in? %>
                    <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
                <% else %>
                    <%= link_to 'Daftar', new_user_registration_path, class: 'dropdown-item' %>
                    <%= link_to 'Login', new_user_session_path, class: 'dropdown-item' %>
                <% end %>
                <div class="dropdown-divider"></div>
              </div>
            </li>
          </ul>
          <form class="form-inline my-2 my-lg-0">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success" type="submit">Search</button>
          </form>
        </div>
      </nav>